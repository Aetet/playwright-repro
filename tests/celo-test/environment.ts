import pkg from "@reatom/core"
const { atom } = pkg

export const hostAtom = atom("", "hostAtom")
export const collectionAtom = atom("", "collectionAtom")
export const blockchainAtom = atom("", "blockchainAtom")
